# 策略表存储 角色 - 资源 - 动作的三者之间的关系
# 也就是 角色-权限表 
# 过程是这样的:
# 根据角色的名称, 在角色表中找到角色id,  再在资源表中找到对应的资源, 
# 添加对应的操作

type Policy {
    _id: ID!
    role_id: ID!
    resource_id: ID!
    act: String
}
type Mutation {
    addPolicy(roleName: String!, resourceName: String!, act: String): IsMutationSuccess
}